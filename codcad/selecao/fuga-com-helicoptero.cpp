#include<iostream>
using namespace std;

int main(){
    int h, p, f, d;
    cin >> h >> p >> f >> d;
    if(d == -1) {
        if(h < p)
            cout << "S";
        else
            cout << "N";
    } else{
        if (fp < fh)
            cout << "S";
        else
            cout << "N";
    }
    return 0;
}
